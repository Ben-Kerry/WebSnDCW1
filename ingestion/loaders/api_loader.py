"""API loader placeholder."""

from typing import Iterable, Mapping


class APILoader:
    def load_teams(self, teams: Iterable[Mapping]) -> None:
        """Load teams into storage/service (placeholder)."""
        for _ in teams:
            pass